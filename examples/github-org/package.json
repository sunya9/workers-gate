{
  "name": "github-org-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "prepare:dev-vars": "test -f .dev.vars || cp .dev.vars.example .dev.vars",
    "typecheck": "pnpm prepare:dev-vars && wrangler types && tsc --noEmit"
  },
  "dependencies": {
    "workers-gate": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.48.0"
  }
}
